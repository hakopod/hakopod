HAKOPOD_API_URL=http://127.0.0.1:8080
# Required in production; generate with: openssl rand -base64 48
HAKOPOD_SESSION_SECRET=
# Required for production behind a TLS reverse proxy. Must be the dashboard origin.
HAKOPOD_WEB_ORIGIN=https://console.example.com
